/*
    This is a very very light version of https://www.npmjs.com/package/dequeue
    
    We would just use this but the project has 0 dependencies and I would prefer to keep it
    this way if at all possible. Still, I ripped off their API ;)
*/

// exporting this since it has the same API as the array... and now we can replace it where we need it very easily
module.exports = Array;
